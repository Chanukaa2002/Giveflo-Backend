import logger from "../utils/logger.js"
import HTTPStatus from "../enum/HTTPStatus.js"

export const DonorRegister = async(req,res)=>{
    try {
        
    } catch (error) {
        logger.error(`Error in auth.controller.DornerRegistor => ${error}`)
        res.status(HTTPStatus.INTERNAL_SERVER_ERROR).json({"error":"Internal server error"})
    }
}
export const OrgRegister = async(req,res)=>{
    try {
        
    } catch (error) {
        logger.error(`Error in auth.controller.DornerRegistor => ${error}`)
        res.status(HTTPStatus.INTERNAL_SERVER_ERROR).json({"error":"Internal server error"})
    }
}
export const Login = async(req,res)=>{
    try {
        
    } catch (error) {
        logger.error(`Error in auth.controller.DornerRegistor => ${error}`)
        res.status(HTTPStatus.INTERNAL_SERVER_ERROR).json({"error":"Internal server error"})
    }
}
export const PasswordReset = async(req,res)=>{
    try {
        
    } catch (error) {
        logger.error(`Error in auth.controller.DornerRegistor => ${error}`)
        res.status(HTTPStatus.INTERNAL_SERVER_ERROR).json({"error":"Internal server error"})
    }
}
export const Me = async(req,res)=>{
    try {
        
    } catch (error) {
        logger.error(`Error in auth.controller.DornerRegistor => ${error}`)
        res.status(HTTPStatus.INTERNAL_SERVER_ERROR).json({"error":"Internal server error"})
    }
}
export const UpdateMe = async(req,res)=>{
    try {
        
    } catch (error) {
        logger.error(`Error in auth.controller.DornerRegistor => ${error}`)
        res.status(HTTPStatus.INTERNAL_SERVER_ERROR).json({"error":"Internal server error"})
    }
}

export const googleReg = async(req,res)=>{
    
}